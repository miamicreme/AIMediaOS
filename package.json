{
  "name": "aimediaos",
  "private": true,
  "version": "0.1.0",
  "description": "AI media operating system for image, video, avatar, and workflow generation.",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  }
}
